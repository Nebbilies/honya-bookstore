route:
  receiver: default
  group_by: ['alertname']
  group_wait: 10s
  group_interval: 1m
  repeat_interval: 1h
receivers:
  - name: default
    webhook_configs:
      - url: http://otel-collector:8889/__noop__
        send_resolved: true
